/**
 * Spotify Web API plumbing for the {@link dev.gamma.modules.misc.Spotify} overlay: the PKCE login,
 * the polled playback snapshot, and the album-art texture cache. Nothing here draws — the overlay
 * itself is {@link dev.gamma.gui.hud.elements.SpotifyElement}.
 */
package dev.gamma.modules.misc.spotify;
