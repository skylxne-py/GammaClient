/**
 * Modules that depend on DonutSMP's own mechanics rather than on vanilla — the
 * {@link dev.gamma.core.Category#DONUT_SMP} section. Grouped by server rather than by function on
 * purpose; see {@link dev.gamma.core.Category} for why.
 */
package dev.gamma.modules.donutsmp;
